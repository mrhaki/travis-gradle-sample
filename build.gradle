apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
    compile 'org.springframework.boot:spring-boot-starter-web:1.1.5.RELEASE'
}

task hello << {
    println 'Simple build file'
    println 'for Travis CI'
}